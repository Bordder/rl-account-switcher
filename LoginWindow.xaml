<ui:FluentWindow x:Class="RLSwitcher.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                 Title="Log into Epic" Width="520" Height="760"
                 Icon="pack://application:,,,/Assets/app.ico"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0" x:Name="Tb" Title="Log into Epic" />

    <Grid Grid.Row="1" Margin="12,2,12,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" x:Name="StatusText" VerticalAlignment="Center"
                 Opacity="0.75" FontSize="12" Text="Sign in with the account you want to add." />
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <ui:ProgressRing x:Name="Busy" IsIndeterminate="True" Width="20" Height="20"
                         Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,10,0" />
        <ui:Button Content="Log out / switch account" Icon="{ui:SymbolIcon ArrowClockwise24}"
                   Click="LogOut_Click" />
      </StackPanel>
    </Grid>

    <wv2:WebView2 Grid.Row="2" x:Name="Web" />
  </Grid>
</ui:FluentWindow>
